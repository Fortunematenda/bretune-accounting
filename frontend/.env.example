# Optional: Set when building for production
# Leave unset for dev (Vite proxy handles /api -> backend)
#
# Production build:
#   VITE_API_URL=https://api.yourdomain.com npm run build
#
# Or create .env.production with:
#   VITE_API_URL=https://api.yourdomain.com
